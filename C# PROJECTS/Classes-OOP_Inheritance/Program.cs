namespace Classes_OOP_Inheritance;
class Program
{
    static void Main(string[] args)
    {
        // CONSIDERATIONS:
            // Inheritance is only supported for classes and interfaces; not structs, delegates, enums
                // NOTE: structs can implement interfaces
            // C# only supported single class inheritance, cannot inherit more than one at once for the same class
                // NOTE: classes can implement multiple interfaces  
    }
}
